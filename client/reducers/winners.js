const initialState =[
  { id: 1, name: 'Alice', score: 40000},
  { id: 2, name: 'Ana', score: 40679},
  { id: 3, name: 'caph', score: 40689}
]

const winners = (state = initialState, action) => {
  switch (action.type) {

    default:
      return state
  }
}

export default winners
